<div class="container mt-3">
    <div class="row">
        <div class="col-md-8">
            <h2>Data Master MataKuliah</h2>
            <a href="/matakuliah/add" class="btn btn-outline-primary ml-auto mb-2"><i class="bi bi-plus-circle"></i> Tambah MataKuliah</a>
          <!-- alert add data success -->
            <% if (msg.length !== 0) { %>
              <div class="alert alert-success" role="alert">
              <%= msg %>
              </div>
            <% } %>
            <table id="example1" class="table table-bordered table-striped">
                <thead style="text-align: center; background-color: #023047;; color: white;  ">
                  <tr>
                    <th scope="col">No</th>
                    <th scope="col">Mata Kuliah</th>
                    <th scope="col">Action</th>
                  </tr>
                </thead>
                <tbody>
                      
                 
                  <% let no = 1; %>
                  <% matakuliahs.forEach(matakuliah => { %>
                    <tr>
                      <td scope="row"><%= no++ %></td>
                      <td><%= matakuliah.matkul %></td>
                      <td>
                          <a href="/matakuliah/<%= matakuliah.matkul %>" class="btn btn-success badge rounded-pill"><i class="bi bi-info-circle"></i> Detail</a>
                      </td>
                    </tr>
                  <% }) %>
                </tbody>
              </table>
               <!-- kondisi jika data masih kosong -->
               <% if (matakuliahs.length === 0) { %>
                <span class="badge badge-danger"><i class="fas fa-info-circle"></i>
                  Data masih kosong, silahkan Menginput data terlebih dahulu!</span>
              <% } %>
        </div>
    </div>
</div>